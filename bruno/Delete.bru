meta {
  name: Delete
  type: http
  seq: 6
}

post {
  url: http://localhost:3000/auth/delete-user
  body: none
  auth: bearer
}

headers {
  cookie: {{authCookie}}
}

auth:bearer {
  token: {{access_token}}
}
